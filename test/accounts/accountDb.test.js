const expect = require("chai").expect
const randomString = require("random-string")
const accountDb = require("../../src/accounts/accountDb")

describe("accountDb", () => {
  //todo add tests after actually having accountDb do something (right now it is just reading and writing a json file)
})
